Notas: abrir terminal ctrl + shift + ñ
clickear en + , Seleccionar perfil predeterminado, seleccionar git bash // siempre va a abrir la terminal en bash

git switch Beta  // cambiar a rama beta

Pushear
git add .
git status
git commit -m "aqui va el mensaje"
git push origin beta                // pushear a rama beta


git pull origin beta  // obtener cambios de otros colaboradores



reestablecer el repositorio a un commit pasado:
git checkout hash del commit

$ git branch alfa

$ git chechout alfa

$ git commit -m "restablecido a commit hash del commit"

git push --force  origin alfa:beta




Notas:
Vamos a trabajar en la rama beta, o igual puedes crear una por separado, luego ya las mergeamos y vamos actualizando el main

Tareas:
1. definir paleta de colores
    Color primario
    Color secundario
    Color grisClaro
    color oscuro
    color detalles

2.definir fuente
    fuente: 

3. Hacer el logo.png e icono .ico de la pagina

4. Haer los layouts
    header
        logos
        navegacion

    main,article
    

    foter


